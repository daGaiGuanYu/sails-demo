extends ../../layouts/main.pug

block title
  title= data?'编辑':'创建'

block body
  form(method="post" action="/item.action")
    .row 标题
      input(name="title")
    .row 详情
      input(name="detail")
    button(type="submit") 提交